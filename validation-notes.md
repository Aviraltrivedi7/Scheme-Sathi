# Current Validation Notes

- 2026-08-20: The `/admin/pilot` development route rendered at desktop width with the Hindi PDF action, folder filter, folder draft field, private saved-view controls, and cohort-report controls visible together without overlap.
- 2026-08-20: The same route rendered at a 375px mobile width. The saved-view controls stacked into one column, and the report actions remained visible and reachable without horizontal clipping.
- The captured administrator screen contained no submitted cohort or dashboard-view records, so the private drag list did not appear in the empty-state visual capture. Its rendering and interaction contract are covered by source-level regression tests.
- 2026-08-20: The desktop `/admin/pilot` render showed the Hindi PDF header/footer input row and the compact Alt-based saved-view shortcut guide without colliding with report actions or saved-view controls.
- 2026-08-20: At 375px, the new print-branding inputs and saved-view shortcut guide stacked cleanly above the saved-view editor. The empty development account again meant folder rename and bulk-move panels had no visible data rows; their protected API and UI contracts are covered by regression tests.
- 2026-08-20: The desktop `/admin/pilot` render showed the expanded Hindi PDF layout controls, including the logo selector and date-format preset, plus the visible Alt+D duplicate action without overlapping the report or saved-view controls.
- 2026-08-20: At 375px, the expanded PDF controls and the duplicate action stacked cleanly with no horizontal clipping. The development account still contains no saved views, so populated folder-count badges are covered by aggregation and UI-contract tests rather than this empty-state capture.
- 2026-08-20: The desktop `/admin/pilot` render showed the Active/Archived saved-view switch, folder color selector, Archive action, and Preview PDF action together without overlap. The test account had no saved views, so populated archive rows, restored state, and colored badges are covered by protected-route and UI-contract tests.
- 2026-08-20: At 375px, the archive controls, folder-color selector, and Preview PDF action stacked without horizontal clipping. The live preview dialog's shared HTML output is covered by helper and UI-contract tests because the visual smoke account is intentionally empty.
- 2026-08-20: The desktop `/admin/pilot` render showed the concise six-color folder legend below the private folder filter, with Active/Archived workspace controls and Preview PDF action remaining readable without collisions.
- 2026-08-20: At 375px, the color legend wrapped cleanly above the view editor and did not create horizontal overflow. The empty development account prevented direct populated archived-list/bulk-restore and preview-dialog interaction capture; those flows are covered by protected router, helper, and UI-contract tests.
- 2026-08-20: The desktop `/admin/pilot` render retained the compact folder legend and saved-view controls without collisions after adding the folder quick-action path. The visual account contains no folders or archived rows, so populated quick actions and retention badges are covered by protected-route and UI-contract tests.
- 2026-08-20: At 375px, the saved-view editor and folder legend remained vertically stacked with no horizontal clipping. The preview page-guide switch is behind the browser-local modal and therefore covered by print-builder/UI-contract tests; no aggregate report, archive, or folder data was created for visual verification.
- 2026-08-20: The desktop `/admin/pilot` render showed the Archive retention explanatory panel, 30-day selector, and disabled-until-needed backup action alongside header/footer left-center-right controls, with no overlap into the saved-view editor.
- 2026-08-20: At 375px, the retention selector, backup action, and expanded PDF customization controls stacked cleanly without horizontal clipping. The test account has no archived rows, so an enabled backup download, applied 15/60-day setting, and live preview alignment rendering are covered by protected route, helper, and UI-contract tests.
