import { categories, states } from "@/lib/schemes";
import { trpc } from "@/lib/trpc";
import { ArrowLeft, CalendarClock, Filter, Search, SlidersHorizontal } from "lucide-react";
import { useMemo, useState } from "react";
import { useLocation } from "wouter";

const dateFormat = new Intl.DateTimeFormat("en-IN", { day: "numeric", month: "short", year: "numeric" });

export default function Discover() {
  const [, setLocation] = useLocation();
  const [query, setQuery] = useState("");
  const [state, setState] = useState("all");
  const [category, setCategory] = useState("all");
  const [deadline, setDeadline] = useState<"all" | "announced" | "closingSoon" | "openEnded">("all");
  const [sort, setSort] = useState<"deadline" | "name" | "category" | "reviewed">("deadline");
  const filters = useMemo(() => ({ query: query || undefined, state: state === "all" ? undefined : state, category: category === "all" ? undefined : category, deadline: deadline === "all" ? undefined : deadline, sort }), [category, deadline, query, sort, state]);
  const catalog = trpc.schemes.list.useQuery(filters, { staleTime: 60_000 });
  const schemes = catalog.data?.schemes ?? [];

  return <main className="discover-page"><header className="discover-header"><button onClick={() => setLocation("/")}><ArrowLeft size={16} /> Back to Scheme Sathi</button><span>ADVANCED DISCOVERY</span></header><section className="discover-intro"><div><p className="desk-kicker"><SlidersHorizontal size={14} /> FIND WITH MORE PRECISION</p><h1>Search by your <em>place, need and deadline.</em></h1><p>Use the latest published scheme details to narrow your list. Always confirm a deadline on the official portal before applying.</p></div><div className="discover-count"><strong>{schemes.length}</strong><span>schemes in this view</span></div></section><section className="discover-workspace"><aside className="discover-filters"><div className="discover-filter-heading"><Filter size={17} /><strong>Refine schemes</strong></div><label>Search<input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Scheme, benefit or category" /></label><label>State / UT<select value={state} onChange={(event) => setState(event.target.value)}><option value="all">All India / any state</option>{states.map((item) => <option key={item} value={item}>{item}</option>)}</select></label><label>Category<select value={category} onChange={(event) => setCategory(event.target.value)}>{categories.map((item) => <option key={item.key} value={item.key}>{item.label}</option>)}</select></label><label>Application deadline<select value={deadline} onChange={(event) => setDeadline(event.target.value as typeof deadline)}><option value="all">Any deadline status</option><option value="closingSoon">Closing within 90 days</option><option value="announced">Deadline announced</option><option value="openEnded">No deadline announced</option></select></label><label>Sort by<select value={sort} onChange={(event) => setSort(event.target.value as typeof sort)}><option value="deadline">Deadline soonest</option><option value="reviewed">Recently reviewed</option><option value="name">Name</option><option value="category">Category</option></select></label></aside><section className="discover-results">{catalog.isLoading && <div className="desk-empty">Loading verified scheme data…</div>}{!catalog.isLoading && schemes.map((scheme) => <article className="discover-scheme-card" key={scheme.id}><div><span className={`category-tag ${scheme.accent}`}>{scheme.category}</span><h2>{scheme.name}</h2><p>{scheme.benefits}</p><div className="discover-meta"><span>{scheme.level} scheme</span><span>{scheme.administeringBody}</span></div></div><aside>{scheme.applicationDeadline ? <div className="discover-deadline"><CalendarClock size={17} /><span><strong>{dateFormat.format(new Date(scheme.applicationDeadline))}</strong><small>{scheme.deadlineLabel}</small></span></div> : <div className="discover-deadline muted"><CalendarClock size={17} /><span><strong>No date announced</strong><small>Check the official portal for the next application window.</small></span></div>}<a href={scheme.portalUrl} target="_blank" rel="noreferrer">Open official portal</a></aside></article>)}{!catalog.isLoading && !schemes.length && <div className="desk-empty"><Search size={24} /><h3>No schemes match these filters.</h3><p>Try another state, category or deadline window.</p></div>}</section></section></main>;
}
